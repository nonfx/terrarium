- id: rdbms
  inputs:
    properties:
      version:
        type: number
      engine:
        type: string
        title: Engine
  outputs:
    properties:
      host:
        type:
      port:

- id: postgres@11
  extends:
    id: rdbms
    inputs:
      version: 11
      engine: postgres

- id: postgres@12
  extends:
    id: rdbms
    inputs:
      version: 12
      engine: postgres

- id: postgres@13
  extends:
    id: rdbms
    inputs:
      version: 13
      engine: postgres
